#include "all.h"
#include <stdio.h>

int main() {
    // printf("%d\n", arr_sortings());
    // printf("%d\n", linked_list());
    // printf("%d", stackMain());
    // printf("returns: %d", queueMain());
    // printf("returns: %d", hashTable());
    printf("\nTesthash: %d\n", testhash());
    printf("\n");
    return 0;
}
